PS.Standalone.IndexRoute = Ember.Route.extend
  beforeModel: ->
    if window.location.origin == '<%= SERVER %>'
      @transitionTo('installed')
    else
      if @get('session.isAuthenticated')
        @transitionTo('tag')
      else
        @transitionTo('login')
