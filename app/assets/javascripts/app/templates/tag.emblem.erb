h1 Select a path to tag to
article.choose-path
  each user_path in model
    single-item item=user_path.path.item filled=true isExpanded=false notes=false sendItemChildren='tagAction'

article.new-path
  | Go to
  <a href='<%= SERVER %>/beta#/paths' target='_blank'> Pathoscope </a>
  | to choose a different path
