
/section.explore
/  article.left
/    - back_collection.each do |title, link|
/      - url = link.is_a?(String) ? link : explore_path(link)
/      = link_to "< #{title}", url, { class: 'path-back' }
/  article.line
/  article.right
/    - if forward_links.present?
/      ul
/      - forward_links.each do |link|
/        li.link
/          div.score.circle
/            = link.nodes.count
/          div.meta
/            p.title
/            = link.title
/            p.actions
/              = link_to link.url, link.url, { class: 'url' }
/          div.deeper
/            = link_to '>', explore_path(link), { class: 'path-forward' }
/        br.cf
/    - else
/      div.dead-***REMOVED***
/        | Draw your own path forward
